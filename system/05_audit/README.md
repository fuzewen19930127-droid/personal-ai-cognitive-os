# Audit Layer

This layer records validation, sensitive-content checks, release readiness, and traceability.

## Recommended Checks

```powershell
npm run validate
npm run scan
npm run dashboard
npm run check
```

## Audit Rule

Do not mark a project as verified until the relevant command or review has actually run.
